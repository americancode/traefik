---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: whoami-gw-basic
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: whoami-gw-basic
  template:
    metadata:
      labels:
        app: whoami-gw-basic
    spec:
      containers:
        - name: whoami
          image: traefik/whoami
          ports:
            - containerPort: 80
