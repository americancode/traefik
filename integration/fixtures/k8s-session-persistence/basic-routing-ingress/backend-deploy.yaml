---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: whoami-ingress-basic
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: whoami-ingress-basic
  template:
    metadata:
      labels:
        app: whoami-ingress-basic
    spec:
      containers:
        - name: whoami
          image: traefik/whoami
          ports:
            - containerPort: 80
