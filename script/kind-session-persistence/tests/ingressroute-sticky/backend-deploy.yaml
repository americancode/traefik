---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: whoami-ir-sticky
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: whoami-ir-sticky
  template:
    metadata:
      labels:
        app: whoami-ir-sticky
    spec:
      containers:
        - name: whoami
          image: traefik/whoami
          ports:
            - containerPort: 80
